<%= form_with(model: questiontag, local: true) do |form| %>
  <% if questiontag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(questiontag.errors.count, "error") %> prohibited this questiontag from being saved:</h2>

      <ul>
        <% questiontag.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :question_id %>
    <%= form.text_field :question_id %>
  </div>

  <div class="field">
    <%= form.label :tag_id %>
    <%= form.text_field :tag_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
